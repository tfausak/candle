{
  "devDependencies": {
    "elm": "0.19.0-bugfix2",
    "elm-format": "0.8.1",
    "elm-live": "3.2.3"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "elm make --optimize src/Main.elm",
    "elm": "elm",
    "elm-format": "elm-format",
    "elm-live": "elm-live",
    "format": "elm-format --yes src",
    "start": "elm-live -- --debug src/Main.elm",
    "test": "elm-format --validate src"
  }
}
