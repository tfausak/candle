{
  "devDependencies": {
    "elm": "0.19.0-no-deps",
    "elm-format": "0.8.1"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "elm make --optimize src/Main.elm",
    "elm": "elm",
    "elm-format": "elm-format",
    "format": "elm-format --yes src",
    "start": "elm reactor",
    "test": "elm-format --validate src"
  }
}
